name: tracer_alumni_v1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  curved_navigation_bar: ^1.0.6
  flutter:
    sdk: flutter
  gap: ^3.0.1
  odoo_rpc: ^0.5.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
